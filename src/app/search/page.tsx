export default function SearchPage() {
  return (
    <div className="container">
      <h1 style={{ fontSize: 'var(--font-size-5)', marginBottom: 'var(--size-4)' }}>
        Search
      </h1>
      <p style={{ color: 'var(--text-2)' }}>
        Search for accounts, posts, and hashtags will be available here.
      </p>
    </div>
  );
}
